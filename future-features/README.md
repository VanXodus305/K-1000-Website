# Post-afe1b0c Feature Archive

`post-afe1b0c-current.tar` is a full snapshot of the repository before the
live site was restored to commit `afe1b0c` on 2026-08-18.

It preserves the recruitment, registration, on-spot, admin, panelist,
superadmin, office, and related supporting code for future reuse.

To inspect or restore it later without touching the active site:

```sh
mkdir -p /tmp/k1000-post-afe1b0c
tar -xf future-features/post-afe1b0c-current.tar -C /tmp/k1000-post-afe1b0c
```
